var link = document.getElementById("<%= params[:html_id] %>");

<% if @invite_created %>
  link.textContent = "Sent";
  link.outerHTML = '<p class="chat-invite-sent chat-invite-btn">' + link.textContent + '</p>';
<% else %>
  link.textContent = "Add";
<% end %>

