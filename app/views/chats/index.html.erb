<h1>Chats index</h1>

<% @chats.each do |chat| %>
  <div>
    <%= link_to chat.name, chat %> 
    <span>host:</span> <%= link_to chat.host.username, chat.host %>
    <%= link_to "Edit", edit_chat_path(chat) %>
  </div>
  <br />
<% end %>